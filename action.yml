name: 'get-tag-action'
description: 'A GitHub action that gets pushed tag name and loads it into an action environment variable'
author: 'ilDug'
# inputs:
#   milliseconds: # change this
#     required: true
#     description: 'input description here'
#     default: 'default value if applicable'
runs:
  using: 'node16'
  main: 'dist/index.js'
